spring:
  jpa:
    show-sql: false
  mail:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    pl.projekt1: INFO
    org.springframework.security: WARN
    org.hibernate: WARN
